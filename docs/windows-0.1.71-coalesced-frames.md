# Windows 0.1.71 显示合并帧诊断

控制端的 WebCodecs 输出和 Canvas 绘制使用一个待显示帧槽位。新帧到达而旧帧尚未绘制时，旧帧会被安全关闭，以避免延迟不断累积。

0.1.71 开始记录这类“显示合并帧”次数。它不是网络丢包，也不代表每一次都需要降画质；它表示解码输出速度曾经超过本地绘制节奏。与显示帧率、最大帧间隔一起查看，可以区分：

- 只有合并帧高：本地绘制节奏较慢，但仍可能保持低延迟。
- 合并帧高且显示帧率低：优先检查 WebView2 或本机 GPU/CPU 负载。
- 合并帧低但中继交付停滞：优先检查网络、中继或目标主机编码。
